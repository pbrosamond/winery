import './BottlingPage.scss';
const { REACT_APP_API_BASE_PATH } = process.env;

function BottlingPage() {
  return (
    <main className='main'>
      
    </main>
  );
};

export default BottlingPage;